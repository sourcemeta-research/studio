export function run(): Promise<void> {
    // This is handled by @vscode/test-cli
    // The test runner will automatically discover and run all test files matching the pattern
    return Promise.resolve();
}
